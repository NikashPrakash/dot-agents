schema_version: 1
delegation_id: del-t2-extend-self-review-skill-1777864417
plan_id: self-review-iteration-close-wiring
task_id: t2-extend-self-review-skill
owner: loop-worker
worker:
    profile: loop-worker
    project_overlay_files:
        - .agents/active/active.loop.md
selection:
    selected_by: workflow fanout
    selected_at: "2026-05-04T03:13:37Z"
    reason: Largest write_scope of self-review plan; isolated from peer plans; pre-decided ADRs (0002, 0003, 0005) make scope concrete
scope:
    write_scope:
        - .agents/skills/dot-agents/self-review/
prompt:
    inline:
        - Extend the self-review skill at ~/.agents/skills/dot-agents/self-review/ (symlinked from .agents/skills/dot-agents/self-review/) per the three accepted ADRs (0002 output schema
        - 0003 fire ordering
        - '0005 kg-context restoration). Concrete deliverables: (1) NEW instructions/kg-context.md as Step 0 — runs ''da kg changes --brief'' and ''da kg impact <files>'' before per-file review; document graceful degradation when KG unavailable. (2) NEW instructions/output-format.md describing review-decision.yaml envelope per ADR-0002 with worked example. (3) MODIFY SKILL.md frontmatter: add ''tier: T2'' and a ''contract:'' block with reads/writes/escape_hatches. (4) MODIFY SKILL.md Workflow section: insert kg-context as Step 0'
        - 'output-format reference as final step. Anti-scope: do NOT touch the existing instruction modules (code-quality'
        - gotchas
        - performance
        - security
        - advisory-board
        - 'checklist) — additions are ADDITIVE. Do NOT touch iteration-close — t3 owns that. Hard test: invoke /self-review on a small real diff'
        - confirm it writes a parseable review-decision.yaml at .agents/active/verification/<task_id>/ with non-stub agent verdicts.
context:
    required_files:
        - docs/adr/0002-self-review-output-schema.md
        - docs/adr/0003-self-review-fire-ordering.md
        - docs/adr/0005-restore-kg-context-in-self-review.md
verification:
    feedback_goal: 'self-review skill extended per ADRs 0002/0003/0005: writes review-decision.yaml at correct path with telemetry envelope schema; restores kg-context Step 0; adds tier+contract frontmatter'
    scenario_tags:
        - self-review-skill-extension
    app_type: skill
closeout:
    worker_must:
        - workflow_verify_record
        - workflow_checkpoint
        - workflow_merge_back
    parent_must:
        - workflow_advance
        - workflow_delegation_closeout
