schema_version: 1
id: sq3-mcp-write-denied
plan_id: sonarqube-pr10
task_id: sq3-security-hotspots-sweep
observation: 'Cannot complete sweep: mcp__sonarqube__change_security_hotspot_status is denied by sandbox permissions (5 attempted calls all returned ''Permission to use ... has been denied''). Read-only MCP tools (search_security_hotspots, show_security_hotspot, get_project_quality_gate_status) work fine. All 35 hotspots are confirmed TO_REVIEW; per-call inspection of all clusters G/H/I is complete (every Go LookPath site is hard-coded, e.g. exec.Command("git", ...); the 3 TS regexes are bounded to file front-matter or single in-repo loop-state.md content; the 4 TS test publicly-writable-dir hotspots use deterministic /tmp paths inside test process). Inspection evidence is preserved in this merge-back. To unblock: grant mcp__sonarqube__change_security_hotspot_status permission to the worker session, then re-run sq3 (the inspection work is cached in the merge-back so the sweep is just per-hotspot status changes). Hotspot keys + dispositions are listed in the merge-back so the operator can flip them by hand if needed.'
classification: small
routed_to: task_note:sonarqube-pr10/sq3-security-hotspots-sweep
created_at: "2026-05-04T04:21:05Z"
