schema_version: 1
id: del-ts-ab-kg-commands-1776194475
parent_plan_id: typescript-port
parent_task_id: ts-ab-kg-commands
title: AB-test B — Implement read-only kg commands in TS port (health, query stub)
summary: Delegated from plan TypeScript Port Plan
write_scope:
    - ports/typescript/src/commands/kg.ts
    - ports/typescript/tests/kg.test.ts
success_criteria: |
    Worker A/B comparison task. Script worker.
    Implement two read-only kg command stubs in TS:
    - `runKgHealth(opts)` → checks KG_HOME env var is set + notes/ subdir exists, returns {healthy, kgHome, warnings[]}
    - `runKgQuery(query, opts)` → returns {query, result: "KG query requires Go CLI — not available in TS port"}
      (intentional stub: marks KG writes as Go-only per phase-4 boundary decision)
    Both take `kgHomeOverride?`. No subprocess or Go CLI invocation.
    Write positive + negative tests in kg.test.ts (missing KG_HOME, empty dir, query stub message).
    Run: `cd ports/typescript && npm test` — all tests must pass.
    feedback_goal: Do the kg health + query stubs correctly reflect the phase-4 boundary decision
    (KG writes are Go-only) with passing tests?
verification_expectations: ""
may_mutate_workflow_state: false
owner: ""
status: completed
created_at: "2026-04-14T19:21:15Z"
updated_at: "2026-04-14T19:25:23Z"
