schema_version: 1
plan_id: go-test-fixture-extraction
tasks:
    - id: t1-audit-and-helper-design
      title: Audit Cluster D fixtures; design canonical helper signatures
      status: completed
      depends_on: []
      blocks:
        - t2-create-internal-testutil
      owner: ""
      write_scope:
        - .agents/workflow/specs/go-test-fixture-extraction/design.md
      verification_required: false
      notes: |-
        Tier: T1 (research + design memo).

        Concrete deliverable: an updated design.md "Open questions"
        section with answers, plus a table of canonical helper
        signatures the rest of the plan implements against.

        Steps:
          1. Re-snapshot live Cluster D duplication
             (mcp__sonarqube__search_duplicated_files on the latest PR
             tip). Counts in design.md drift across pushes.
          2. For each file in the inventory, list every fixture
             function it defines (setupTempProject, writeFixtureRC,
             manifest writers, etc.). Group by signature.
          3. Identify cases where a per-package fixture is
             materially different (e.g. graphstore tests need a real
             SQLite/Postgres handle; workflow tests need a populated
             plan tree). These get bucket-specific helpers.
          4. Write the signature table — return values, error policy
             (t.Fatal vs returned error), TempDir ownership.

        Hard test: design.md "Open questions" section is empty;
        signature table covers every duplicated block in the live
        SonarCloud inventory.

        Anti-scope: do NOT begin extracting helpers in this task.
        Audit only. The audit may surface a need to split this plan
        further (e.g. graphstore fixture work as its own plan); raise
        via fold-back rather than expanding scope here.

        ISP routing:
          mode: direct (research + spec edit)
          verifier: doc review
          review: design quality (signatures must compose; no
                  package-cycle hazards)
          bundle-context (if fanout): findings.md Cluster D table,
                                       the 21 *_test.go files
          output contract: design.md updated with concrete signatures
      app_type: research
    - id: t2-create-internal-testutil
      title: Land internal/testutil with helpers + unit tests
      status: completed
      depends_on:
        - t1-audit-and-helper-design
      blocks:
        - t2_5-extend-internal-testutil
        - t3-extract-platform-tests
        - t4-extract-workflow-tests
        - t5-extract-commands-root-tests
        - t6-extract-graphstore-tests
        - t7-extract-skills-and-agents-tests
      owner: ""
      write_scope:
        - internal/testutil/
      verification_required: true
      notes: |-
        Tier: T2 (compound — new package + unit tests).

        Implements the helper set converged on in T1. Helpers must:
          * accept *testing.T as first arg, call t.Helper()
          * t.Fatal on prerequisite errors (matches existing style)
          * not call t.TempDir() internally (see design.md decision)
          * have go-doc on every exported function

        Unit tests cover helper logic with non-trivial branching
        (e.g. WriteAgentsRC with explicit Sources vs default).
        Helpers that are pure thin wrappers around os.WriteFile
        don't need a unit test; they're exercised by every consumer.

        Hard test: go test ./internal/testutil passes; package builds
        clean; no consumers yet (extraction starts in T3+).

        Anti-scope: do NOT modify any *_test.go in this task. Land
        the helpers; consumers come next.

        ISP routing:
          mode: direct (single-package implementation)
          verifier: smoke (go test ./internal/testutil)
          review: code review
          bundle-context (if fanout): T1's signature table
          output contract: internal/testutil exists with helpers,
                           docs, tests; importable by any *_test.go
      app_type: code-fix
    - id: t2_5-extend-internal-testutil
      title: 'Extend internal/testutil: NewTempAgentsHome + WriteScopeFilePath'
      status: completed
      depends_on:
        - t2-create-internal-testutil
      blocks:
        - t3-extract-platform-tests
        - t6-extract-graphstore-tests
      owner: ""
      write_scope:
        - internal/testutil/testutil.go
        - internal/testutil/testutil_test.go
      verification_required: true
      notes: |-
        Tier: T2 (additive helper extension).

        Two new helpers, both surfaced by t5's merge-back narrative
        (commands/refresh_test.go ×17, commands/import_test.go ×10)
        and corroborated by t7's audit
        (commands/skills/promote_test.go::TestPromoteSkillIn_Mirror...
        site). Landing these BEFORE t3 and t6 prevents those workers
        from hitting the same walls and re-surfacing the same finding
        as t5.5 candidates.

        Helper 1 — NewTempAgentsHome:

          // NewTempAgentsHome creates a t.TempDir(), sets HOME and
          // AGENTS_HOME envs (with .agents/ subdir under HOME), and
          // returns (tmp, agentsHome).
          //
          // Lighter than NewTempProject when a test needs full env-
          // var control but no project tree. Absorbs the recurring
          // 5-line preamble:
          //
          //   tmp := t.TempDir()
          //   t.Setenv("HOME", tmp)
          //   agentsHome := filepath.Join(tmp, ".agents")
          //   if err := os.MkdirAll(agentsHome, 0755); err != nil { t.Fatal(err) }
          //   t.Setenv("AGENTS_HOME", agentsHome)
          //
          // Use NewTempProject when you also need a project dir +
          // canonical agents tree; use this when you don't.
          func NewTempAgentsHome(t *testing.T) (tmp, agentsHome string)

        Helper 2 — WriteScopeFilePath:

          // WriteScopeFilePath extends WriteScopeFile to accept a
          // nested relative path under agentsHome/bucket/scope/, e.g.
          // ".github/hooks/pre-tool.json". The relative path components
          // are MkdirAll'd as needed.
          //
          // Equivalent to WriteScopeFile when relPath has no
          // separator. Unlocks the seed loop in
          // TestRestoreFromResourcesCounted_RestoresDirectoryBuckets
          // and similar nested-path call sites surfaced in t5's
          // merge-back.
          func WriteScopeFilePath(t *testing.T, agentsHome, bucket, scope, relPath string, content []byte)

        Implementation rules (per t2's design):
          * t.Helper() first
          * t.Fatal on prerequisite errors
          * GoDoc on every exported function
          * NewTempAgentsHome may call t.TempDir() internally
            (different from t2's "no t.TempDir() inside helpers"
            rule because the env-var setup wraps the temp dir; this
            is the explicit purpose of the helper)
          * No call-site migrations in this task — those happen in
            t3/t6 (which already own the affected test files)

        Hard test: go test ./internal/testutil -race -count=1 passes;
        package builds clean.

        Anti-scope: do NOT modify any consumer *_test.go in this
        task. Just land the helpers + their unit tests. t3 / t6 / a
        possible future cleanup pass consume them.

        ISP routing:
          mode: direct (single-package additive)
          verifier: smoke (go test ./internal/testutil -race -count=1)
          review: code review (small surface)
          bundle-context (if fanout): t5 merge-back
            (.agents/history/archived-delegations/2026-05-23/
            t5-pr49-orphans/t5-extract-commands-root-tests.md) for
            the exact call-site shape; t2's design doc for the
            helper-style rules.
          output contract: two new exported helpers + their unit
            tests, ready for t3/t6 consumers.
      app_type: code-fix
    - id: t3-extract-platform-tests
      title: Refactor internal/platform/*_test.go to use internal/testutil
      status: completed
      depends_on:
        - t2-create-internal-testutil
        - t2_5-extend-internal-testutil
      blocks: []
      owner: ""
      write_scope:
        - internal/platform/resource_plan_test.go
        - internal/platform/mcp_settings_test.go
        - internal/platform/rules_test.go
      verification_required: true
      notes: |-
        Tier: T2 (compound — three files; resource_plan_test.go is
        the largest single Cluster D offender at 239 dup lines).

        Refactor each test file to consume internal/testutil
        helpers in place of in-file setup/fixture-writer functions.
        Delete now-unused per-file helpers.

        Per-file commit convention (one commit each):
          * "test(platform): use internal/testutil in resource_plan_test"
          * "test(platform): use internal/testutil in mcp_settings_test"
          * "test(platform): use internal/testutil in rules_test"

        Hard test: go test ./internal/platform/ passes after each
        commit; commit-by-commit diff shows test bodies reduced
        by N lines with assertions unchanged.

        Common false positive: refactor accidentally drops an
        assertion when collapsing a per-file helper. Reviewer must
        compare assertion lists pre/post.

        Anti-scope: do NOT touch production code in this task.

        ISP routing:
          mode: fanout-amenable (one subagent per file)
          verifier: smoke (go test ./internal/platform/)
          review: code review per file
          bundle-context (if fanout): T1's signature table, T2's
                                       internal/testutil API
          output contract: ~250 dup lines removed; gate-relevant
                           density falls
      app_type: code-fix
    - id: t4-extract-workflow-tests
      title: Refactor commands/workflow/*_test.go to use internal/testutil
      status: completed
      depends_on:
        - t2-create-internal-testutil
      blocks: []
      owner: ""
      write_scope:
        - commands/workflow/state_plan_test.go
        - commands/workflow/testutil_test.go
        - commands/workflow/workflow_integration_test.go
        - commands/workflow/delegation_fanout_test.go
        - commands/workflow/iter_log_test.go
        - commands/workflow/foldback_test.go
        - commands/workflow/plan_task_test.go
        - commands/workflow/drift_sweep_test.go
      verification_required: true
      notes: |-
        Tier: T2 (compound — eight files; second-largest cluster).

        commands/workflow/testutil_test.go itself is a per-package
        helper file that should be deleted or reduced to a thin
        re-export shim around internal/testutil. Verify its API is
        the same as internal/testutil's before deleting.

        One commit per file. Hard test per commit:
        go test ./commands/workflow/.

        Anti-scope: no plan-engine behavioral changes; this task
        only moves test scaffolding. If a state_plan_test scenario
        is genuinely broken under the new helpers, fold-back.

        ISP routing:
          mode: fanout-amenable (one subagent per file)
          verifier: smoke (go test ./commands/workflow/)
          review: code review per file
          bundle-context (if fanout): T1's signature table, T2's
                                       internal/testutil API
          output contract: ~610 dup lines removed
      app_type: code-fix
    - id: t5-extract-commands-root-tests
      title: Refactor commands/*_test.go (root) to use internal/testutil
      status: completed
      depends_on:
        - t2-create-internal-testutil
      blocks: []
      owner: ""
      write_scope:
        - commands/refresh_test.go
        - commands/import_test.go
        - commands/mcp_settings_test.go
        - commands/rules_test.go
      verification_required: true
      notes: |-
        Tier: T2 (compound — four files).

        commands/refresh_test.go is a high-density single file
        (32.1%, 114 dup lines) with substantial fixture overlap
        with refresh.go's setup. commands/mcp_settings_test.go
        (44.2%, 46 dup lines) is the highest-density per-file
        offender — its fixture pattern likely mirrors
        internal/platform/mcp_settings_test.go and may share a
        helper.

        One commit per file. Hard test per commit: go test ./commands/.

        ISP routing:
          mode: serialized single worker (4 files sequentially)
          verifier: smoke (go test ./commands/)
          review: code review per file
          bundle-context (if fanout): T1's signature table, T2's
                                       internal/testutil API
          output contract: ~230 dup lines removed

        ---
        Fanout briefing (2026-05-23 — orchestrator re-pickup):

        Pre-existing in_progress status carries no in-flight worker,
        no branch, no bundle; treat as a fresh spawn.

        Required reads BEFORE touching any target file:
          1. internal/testutil/testutil.go — the canonical helper API
             shipped by t2 (NewTempProjectDir, fixture seeders, etc.).
          2. .agents/history/archived-delegations/2026-05-23/t4-extract-workflow-tests.yaml
             — the immediately preceding sibling bundle (t4, PR #44
             MERGED) — same plan, same refactor pattern, same
             forbidden_scope shape, same verification commands. Mirror
             its commit-per-file rhythm.

        Worktree: create at .agents/worktrees/t5 off master (matches
        current repo convention; .claude/worktrees/ was cleaned up
        2026-05-23 and is no longer used).

        Branch: t5-extract-commands-root-tests off master. Fresh
        branch, NOT stacked on any other plan's branch.

        One-PR-in-flight: open PR after all 4 commits land on the
        branch; do not merge — user gates.

        Touch tests only — no production .go changes. If you find a
        helper in internal/testutil that would benefit from a small
        additive extension to absorb a duplicated pattern, STOP and
        report (out of write_scope; do not silently modify the
        shared package). Same rule for production code: report, do
        not fix-and-pad.
      app_type: code-fix
    - id: t6-extract-graphstore-tests
      title: Refactor internal/graphstore/*_test.go to use internal/testutil
      status: completed
      depends_on:
        - t2-create-internal-testutil
        - t2_5-extend-internal-testutil
      blocks: []
      owner: ""
      write_scope:
        - internal/graphstore/sqlite_test.go
        - internal/graphstore/postgres_test.go
        - internal/graphstore/mcp_server_test.go
      verification_required: true
      notes: |-
        Tier: T2 (compound — three files; may need a graphstore
        sub-helper per T1 audit's open question).

        Open question carried from spec: do graphstore tests share
        enough fixture shape with the broader corpus to use the
        canonical helpers, or do they need a graphstore-specific
        test util? T1 audit must answer this before T6 starts.

        If a graphstore sub-helper is needed, land it in
        internal/graphstore/internal/storetest/ (not exported).

        One commit per file. Hard test per commit:
        go test ./internal/graphstore/.

        Anti-scope: do NOT touch graphstore production code; this
        task only moves test scaffolding.

        ISP routing:
          mode: direct (storage tests are subtle; sequential
                        processing reduces flake risk)
          verifier: smoke (go test ./internal/graphstore/)
          review: code review per file
          bundle-context (if fanout): T1's signature table, T2's
                                       API, the open-question
                                       resolution from T1
          output contract: ~156 dup lines removed
      app_type: code-fix
    - id: t7-extract-skills-and-agents-tests
      title: Refactor commands/{agents,skills,kg}/*_test.go to use internal/testutil
      status: completed
      depends_on:
        - t2-create-internal-testutil
      blocks: []
      owner: ""
      write_scope:
        - commands/agents/agents_test.go
        - commands/skills/promote_test.go
        - commands/kg/kg_test.go
      verification_required: true
      notes: |-
        Tier: T2 (compound — three files).

        commands/agents/agents_test.go and commands/skills/promote_test.go
        already use setupAgentsEnv/setupSkillsEnv per-file. After
        projectsync.{ListBucket,PromoteResource} extraction landed
        in PR#10, the bucket spec is uniform; per-file fixtures
        should collapse cleanly to internal/testutil's bucket-aware
        helpers (NewTempProject + WriteManifest variants).

        commands/kg/kg_test.go's 91 dup lines spread across 11
        blocks — many small repetitions; refactor may need
        table-driven tests in addition to fixture extraction.

        One commit per file. Hard test per commit:
        go test ./commands/agents ./commands/skills ./commands/kg.

        ISP routing:
          mode: fanout-amenable (one subagent per file)
          verifier: smoke (focused go test)
          review: code review per file
          bundle-context (if fanout): T1's signature table, T2's
                                       API, projectsync.PromoteSpec
                                       and BucketSpec field shapes
          output contract: ~318 dup lines removed
      app_type: code-fix
    - id: t6b-extend-storetest
      title: Extend internal/graphstore/internal/storetest with Metadata/Edge/NoteSymbolLink runners
      status: in_progress
      depends_on:
        - t6-extract-graphstore-tests
      blocks:
        - t6c-graphstore-tests-storetest-migrate
        - t8-archive
      owner: dot-agents
      write_scope:
        - internal/graphstore/internal/storetest/
      verification_required: true
      notes: |-
        Surfaced by t6 worker's fold-back (PR #84): bundle prompt
        said use internal/testutil but design.md audit
        categorizes sqlite_test.go + postgres_test.go as
        Graphstore sub-helper (storetest, not testutil). t2 shipped
        storetest with RunNodeRoundTrip / RunKGNoteRoundTrip /
        RunKGNoteSearch runners; remaining cross-file dup needs
        three more parameterizable runners.

        Tier: T2 (extend existing package).

        New runners to add to internal/graphstore/internal/storetest:
          - RunMetadataRoundTrip(t, store, keyPrefix string)
            — covers Metadata_RoundTrip + Metadata_MissingKey +
              Metadata_Overwrite (3 mirrored tests, ~30 LOC/backend).
          - RunEdgeUpsertCreate(t, store, idPrefix string)
            RunEdgeUpsertUpdate(t, store, idPrefix string)
            — covers UpsertEdge_Create + UpsertEdge_Update
              (2 mirrored tests, ~24 LOC/backend).
          - RunNoteSymbolLinkRoundTrip(t, store, namePrefix string)
            RunNoteSymbolLinkIdempotent(t, store, namePrefix string)
            — covers NoteSymbolLink_RoundTrip + _Idempotent
              (2 mirrored tests, ~30 LOC/backend).

        Each runner takes a prefix parameter so the shared Postgres
        testcontainer (cross-backend run) doesn't see row collisions
        — t2's existing runners already pin this pattern. Verify by
        running storetest from both sqlite_test.go and
        postgres_test.go simultaneously without test interaction.

        Anti-scope: do NOT modify the consumer files
        (sqlite_test.go, postgres_test.go) — that's t6c. Only
        ship the runners + their isolation contract.

        Hard test: go test ./internal/graphstore/internal/storetest -count=1
        green; the new runner functions are exported (PascalCase)
        and documented.

        Cross-plan: pre-existing TestCRGBridge* env-dependent
        failure remains (unrelated to t6b; requires python module
        on PATH).
      app_type: code-fix
    - id: t6c-graphstore-tests-storetest-migrate
      title: Migrate sqlite_test.go + postgres_test.go onto extended storetest runners
      status: pending
      depends_on:
        - t6b-extend-storetest
      blocks:
        - t8-archive
      owner: dot-agents
      write_scope:
        - internal/graphstore/sqlite_test.go
        - internal/graphstore/postgres_test.go
      verification_required: true
      notes: |-
        Consumer migration onto the runners landed by t6b.
        Per-file commit: one for sqlite, one for postgres.
        Replace each mirrored test pair (Metadata, Edge, NoteSymbolLink)
        with a single line invoking the storetest runner with the
        appropriate key/id/name prefix. Existing storetest runners
        (RunNodeRoundTrip / RunKGNoteRoundTrip / RunKGNoteSearch
        from t2) stay in place; this task only collapses the t6b-
        addressed dup.

        Hard test per commit: go test ./internal/graphstore -race -count=1
        green; visible dup-line reduction in each consumer file.

        Anti-scope: do NOT touch storetest itself (t6b owns).
        Tests-only refactor; no production .go changes.
      app_type: code-fix
    - id: t8-archive
      title: Open follow-up PR; verify gate; archive plan
      status: pending
      depends_on:
        - t3-extract-platform-tests
        - t4-extract-workflow-tests
        - t5-extract-commands-root-tests
        - t6-extract-graphstore-tests
        - t6b-extend-storetest
        - t6c-graphstore-tests-storetest-migrate
        - t7-extract-skills-and-agents-tests
      blocks: []
      owner: ""
      write_scope:
        - .agents/history/go-test-fixture-extraction/
      verification_required: false
      notes: |-
        Tier: T1 (closeout).

        Open the follow-up PR off master with all extraction
        commits stacked. Verify:
          * mcp__sonarqube__get_project_quality_gate_status reports
            new_duplicated_lines_density below 3% OR Cluster D
            contribution reduced by >= 60%
          * go test ./... passes
          * Cross-link to ADR-0008 in the PR description

        On merge: archive plan via dot-agents workflow plan archive
        --plan go-test-fixture-extraction. Bundles PLAN.yaml,
        TASKS.yaml, plan.md, and per-task merge-back artifacts.

        Anti-scope: do NOT bundle production-code-helper-extraction
        commits into the same PR. Cluster D and Cluster E.other are
        independent plans by design (ADR-0008).

        ISP routing:
          mode: direct (closeout)
          verifier: smoke (gate snapshot + go test)
          review: closeout
          output contract: history dir populated; ADR-0008 linked
                           from PR description
      app_type: workflow
